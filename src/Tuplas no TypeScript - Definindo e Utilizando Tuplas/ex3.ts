// 3. **Exercício 3:** Declare uma tupla que contenha um nome (string), idade (number) e um status de
// membro ativo (boolean). Atribua valores e altere o valor da idade. Imprima os valores atualizados.
let membro: [string, number, boolean] = ["Leonardo", 38, true];

membro[1] = 40;

console.log(membro);