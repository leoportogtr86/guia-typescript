// 5. **Crie um type alias `Coordenadas` para representar uma tupla com dois números.
// Declare uma variável do tipo `Coordenadas` e atribua valores.**
type Coordenadas = [number, number];

const coord2: Coordenadas = [100.45, 1234.789];

console.log(coord2);