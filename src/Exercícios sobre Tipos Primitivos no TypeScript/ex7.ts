// 7. **Exercício 7:** Escreva uma função chamada `logarMensagem` que receba uma mensagem do tipo `string`
// e não retorne nenhum valor. Use o tipo de retorno `void`.
const logarMensagem = (msg: string): void => {
    console.log(msg);
}

logarMensagem("Olá, mundo!");