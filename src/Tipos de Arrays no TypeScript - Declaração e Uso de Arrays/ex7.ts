// Use map para criar um novo array que contenha o dobro dos valores de um array de números existente.
const n: number[] = [10, 20, 30];
const dobro: number[] = n.map((e) => e * 2);

console.log(dobro);