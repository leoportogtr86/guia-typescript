// 4. **Exercício 4:** Crie um enum chamado `EstadoCivil` com os valores `Solteiro`, `Casado`, `Divorciado` e
// `Viuvo`. Declare uma variável do tipo `EstadoCivil` e atribua a ela o valor `EstadoCivil.Casado`.
// Imprima o valor no console.
enum EstadoCivil {
    Solteiro,
    Casado,
    Divorcidado,
    Viuvo
}

const estadoCivil: EstadoCivil = EstadoCivil.Casado;